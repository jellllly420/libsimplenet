{
    "name": "libsimplenet-dev",
    "image": "mcr.microsoft.com/devcontainers/cpp:latest",
    "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
    "runArgs": [
        "--network=host"
    ],
    "remoteUser": "vscode",
    "containerEnv": {
        "SHELL": "/bin/zsh"
    },
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y --no-install-recommends liburing-dev libboost-dev libboost-system-dev gcovr clangd clang-format clang-tidy hyperfine valgrind linux-perf doxygen graphviz && sudo apt-get clean && sudo rm -rf /var/lib/apt/lists/*"
}
